name: onet
channels: [pytorch, nvidia, conda-forge]
dependencies:
  - python=3.10
  - pip
  - pip:
      - torch
      - torchvision
      - open_clip_torch>=2.24
      - timm
      - pillow
      - pandas
      - numpy
      - scikit-learn
      - tqdm
      - pyyaml
      - matplotlib
